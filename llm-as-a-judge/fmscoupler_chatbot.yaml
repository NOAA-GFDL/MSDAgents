What is xmap_sfc?: xmap_sfc is a FmsXgridXmap_type variable that holds the exchange
  grid between different components in the FMSCoupler. It facilitates data exchange
  between the atmosphere, land, and ice grids by mapping data onto this exchange grid
  for computation before interpolating it onto the receiving component grid.
What is the register id for  'moisture scale'?: id_q_star
What is in atm_land_ice_flux_exchange_mod?: "The `atm_land_ice_flux_exchange_mod`\
  \ contains:\n\n- A derived type `tracer_ind_type` with members:\n  - `atm`: integer\n\
  \  - `ice`: integer\n  - `lnd`: integer\n\n- Another derived type `tracer_exch_ind_type`\
  \ with members:\n  - `exch`: integer\n  - `ice`: integer\n  - `lnd`: integer\n\n\
  - A public subroutine `atm_land_ice_flux_exchange_init` which initializes the module\
  \ by allocating memory and setting default values for module-level variables, and\
  \ calling initialization routines in FMS modules.\n\n- Two subroutines under `put_logical_to_real`:\n\
  \  - `put_logical_to_real_sg`\n  - `put_logical_to_real_ug`\n\nThis module handles\
  \ flux exchange between the atmosphere and land/ice grids via the exchange grid."
Where is t_surf registered for diag_manager?: t_surf is registered with `register_global_diag_field`
  as a global scalar time-series field.
How is the code different between using and not using the AM3_PHYS macro: "When `use_AM3_physics`\
  \ is defined, the following changes occur in the FMSCoupler:\n\n1. **No compilation\
  \ of `shflx` and `lhflx`**: The sensible heat flux (`shflx`) and latent heat flux\
  \ (`lhflx`) are not compiled when using AM3 physics.\n\n2. **Different computation\
  \ for `ex_t_surf_miz`**:\n   - When `use_AM3_physics` is defined, `ex_t_surf_miz`\
  \ (surface temperature in the marginal ice zone on the exchange grid) is used only\
  \ in forecast mode (`do_forecast=.true.`) with `#ifdef AM3_physics`.\n   - Without\
  \ `use_AM3_physics`, there's no specific mention of `ex_t_surf_miz`'s usage or compilation.\n\
  \nThese differences suggest that when using AM3 physics, the heat and moisture exchange\
  \ between the atmosphere and surface might be handled differently compared to when\
  \ AM3 physics is not used."
What is _USE_LEGACY_LAND?: _USE_LEGACY_LAND is a macro that, when set at compile time,
  indicates the use of legacy land model components in the FMSCoupler. It affects
  the registration of certain diagnostic fields and the usage of specific subroutines
  like `put_logical_to_real_ug`.
What does sfc_boundary_layer do?: "`sfc_boundary_layer` computes and exchanges fluxes\
  \ at the surface between atmosphere and land/ice/ocean components. It handles:\n\
  \n- Computing fluxes: `t_surf_atm`, `albedo_atm`, `rough_mom_atm`, `land_frac_atm`,\
  \ `dtaudu_atm`, `dtaudv_atm`, `flux_u_atm`, `flux_v_atm`, `u_star_atm`, `b_star_atm`\n\
  - Transferring fluxes to exchange grid for different boundaries:\n  - Atmosphere\
  \ boundary: `t_bot`, `z_bot`, `p_bot`, `u_bot`, `v_bot`, `p_surf`, `slp`, `gust`,\
  \ coupler bc type fields\n  - Ice boundary: `t_surf`, `rough_mom`, `rough_heat`,\
  \ `rough_moist`, `albedo`, `u_surf`, `v_surf`\n  - Land boundary: `t_surf`, `t_ca`,\
  \ `rough_mom`, `rough_heat`, `albedo`, `tracers`\n- It also computes heat stocks\
  \ from radiative and turbulent fluxes, and heat carried by river and PME (precipitation\
  \ minus evaporation)."
There are all these stock computations.  What is a stock and why are they needed?: 'A
  "stock" in the context of FMSCoupler refers to the globally integrated total amount
  of conserved quantities such as mass (e.g., water), heat, or salt within each climate
  component (atmosphere, land, ice, ocean). Stocks are used to check conservation,
  i.e., to ensure that these quantities are neither created nor destroyed but only
  transferred between components. They are needed for:


  1. **Conservation Check**: To verify that the model is conserving mass, heat, and
  salt according to physical laws.

  2. **Diagnostic Tool**: Stocks help diagnose errors or inconsistencies in the model''s
  flux calculations.

  3. **Model Validation**: Comparing stock changes with respect to initial values
  helps validate the model''s performance over time.


  In FMSCoupler, stocks are initialized (flux_init_stocks), updated after flux transfers
  (e.g., flux_ocean_from_ice_stocks), and checked for conservation (flux_check_stocks)
  at specified intervals or at the end of the run based on the `check_stocks` namelist
  flag.'
What do 'sg' and 'ug' suffixes stand for throughout this file?: '''sg'' stands for
  Structured Grid, while ''ug'' stands for Unstructured Grid. These suffixes are used
  to differentiate between the two types of grids in the context of the FMSCoupler.'
What is diag_field_init?: diag_field_init is a subroutine that registers diagnostic
  fields in the module to the diag_manager. It ensures these fields are registered
  correctly so their data can be outputted to a NetCDF file at the end of the model
  run. This subroutine is called during module initialization in subroutine atm_land_ice_flux_exchange_init.
What is flux_ex_arrays_dealloc?: flux_ex_arrays_dealloc is a public subroutine in
  FMSCoupler. It deallocates the model-level exchange grid related arrays that were
  allocated earlier, specifically in the sfc_boundary_layer module. This subroutine
  is called after the remapping of atmosphere gas/tracer fields onto the exchange
  grid and computation of air-sea deposition fluxes to free up memory by releasing
  the allocated resources for these exchange grid arrays.
Where is program main in a coupled model?: The `main` program for FMSCoupler is not
  explicitly stated in the provided context. However, it's mentioned that `coupler_main`
  contains the main time loops to call the time-stepping dynamics and flux exchange
  between components. Therefore, `coupler_main` could be considered as the main program
  for the coupled model.
How do I specify the starting model time?: You can specify the starting model time
  using the `date_init` variable. It is an integer array with dimensions `(6)`, representing
  the initial date for the model run in the format `(yr, mo, day, hr, min, sec)`.
  For example, to start a run on 1 January 2000 at 00:00:00, you would set `date_init
  = (/2000, 1, 1, 0, 0, 0/)`
How do I specify the length of the runtime?: 'You can specify the length of the runtime
  using namelist variables:


  - For additional seconds: Set `seconds` variable.

  - For additional minutes: Set `minutes` variable.

  - For additional hours: Set `hours` variable.'
How do I restart a model?: "To restart a model using FMSCoupler, you need to call\
  \ `coupler_restart` function with appropriate arguments. Here's how:\n\n1. Ensure\
  \ that the necessary input/output variables are set correctly:\n   - `time_current`:\
  \ The current model time.\n   - `time_start`: The model start time.\n   - `time_restart_current`:\
  \ The next restart time.\n\n2. Call `coupler_restart` function with these arguments\
  \ to write coupler restart files (`RESTART/coupler.res`) and component restart files\
  \ for atmosphere, ocean, land, and ice models.\n\nHere's a simplified example:\n\
  \n```fortran\ncall coupler_restart(time_current, time_start, time_restart_current)\n\
  ```\n\nAfter this call, the model will be restarted from the `time_restart_current`\
  \ time."
Do I need to set any namelist parameter to restart a run?: Yes, you need to set `do_endpoint_chksum`
  to `.TRUE.` in the namelist to perform checksums of the initial and final states
  when restarting a run. Additionally, ensure that restart files exist for each ensemble
  member if it's an ensemble run (`ensemble_size`).
What happens in the coupler?: "The FMSCoupler facilitates communication between atmosphere,\
  \ ocean, land, and ice components in GFDL's coupled climate models. Here are some\
  \ key processes that happen within the coupler:\n\n1. **Flux Exchange**: The `coupler_main`\
  \ subroutine manages the main time loops to advance the coupled model and calls\
  \ flux exchange routines between all components. Fluxes exchanged include heat,\
  \ momentum (wind stresses), freshwater (precipitation minus evaporation), and others.\n\
  \n2. **Clock Management**:\n   - `coupler_clocks`: Keeps track of time steps for\
  \ each component.\n   - `time_restart` & `time_restart_current`: Manage intermediate\
  \ restart times in the coupler.\n   - `current_timestep`: Tracks the current timestep\
  \ during model integration.\n\n3. **Initialization and Finalization**:\n   - `coupler_init`:\
  \ Initializes all component models, flux-exchange infrastructure, and runtime configurations\
  \ before time stepping loops begin.\n   - `termination` clock: Signals the end of\
  \ the coupler's finalization phase (`coupler_end`).\n\n4. **Stock Updates**: Subroutine\
  \ `flux_ocean_from_ice_stocks` updates heat stocks in the Ocean component after\
  \ receiving fluxes from Ice, using internally consistent boundary fluxes.\n\n5.\
  \ **Boundary Conditions**:\n   - `Ice_ocean_boundary%wind_stagger`: Specifies how\
  \ wind stresses are discretized spatially.\n   - `Ice_ocean_boundary%u10_sqr`: Provides\
  \ wind speed squared at 10m height for NUOPC cap only."
What happens in the fast loop?: "In the fast loop (atmos_loop), the atmosphere, land\
  \ surface, and fast sea-ice are advanced by one atmospheric timestep (Time_step_atmos).\
  \ Here's what happens:\n\n1. **Atmospheric downward sweep**: Heat and moisture are\
  \ exchanged between the atmosphere and the surface using a tridiagonal scheme for\
  \ implicit vertical diffusion.\n   - `coupler_update_atmos_model_down`: Forward\
  \ (downward) sweep from atmospheric top to surface.\n   - `coupler_flux_down_from_atmos`:\
  \ Transfers forward-elimination coefficients to land/ice.\n\n2. **Land and ice fast\
  \ updates**:\n   - `coupler_update_land_model_fast`: Computes new surface temperatures\
  \ for land.\n   - `coupler_update_ice_model_fast`: Computes new surface temperatures\
  \ for fast sea-ice.\n\n3. **Atmospheric diagnostics and tracer state update**: `update_atmos_model_state`\
  \ is called to advance atmospheric diagnostics and tracer state.\n\n4. **Fast-ice\
  \ accumulators reset and boundary exchange**:\n   - `ice_model_fast_cleanup`: Resets\
  \ fast-ice accumulators.\n   - `unpack_land_ice_boundary`: Copies Land_ice_boundary\
  \ into ice internals.\n   - `exchange_fast_to_slow_ice`: Exchanges fast-ice averages\
  \ to the slow-ice side."
What happens in the slow loop?: 'In the slow loop, also known as the coupled time
  loop (controlled by `main`), the ocean and slow sea-ice models advance by one coupled
  timestep (`dt_cpld`). Here''s what happens:


  1. **Ocean advancement**: The ocean model takes a step forward based on its internal
  dynamics and physics.

  2. **Slow sea-ice advancement**: The slow sea-ice model (Ice%slow_ice_pe) advances,
  handling dynamics, freezing/melting, and transport at the coupled timestep.

  3. **Flux exchange**: Ocean-ice fluxes are exchanged once per iteration of the slow
  loop.


  The slow loop iterates `num_cpld_calls` times, with each iteration tracked by the
  counter `nc`. The time tracked for the ocean (`Time_ocean`) and flux exchange from
  ocean to ice (`Time_flux_ocean_to_ice`) is also updated in this loop.'
What is do_concurrent_radiation?: do_concurrent_radiation is a logical, public variable
  that acts as a namelist flag. If it's set to .TRUE., radiation calculations are
  updated concurrently with atmospheric physics using OpenMP threading, with the number
  of threads specified by radiation_nthreads.
Does ice run on atm pes?: No, Ice does not run on Atm%pe PEs. Fast ice runs on a subset
  of the atmosphere PEs (`Ice%fast_ice_pe`), but slow ice runs on its own PE set (`Ice%slow_ice_pe`).
Where are the outputs?: The outputs of subroutine `flux_check_stocks` are written
  to the logfile. The stock differences with respect to the initial values for atm,
  land, ice, and ocean components are computed and logged.
How do I specify where the inputs are?: "In the provided context, you can specify\
  \ where the inputs are by using the following arguments:\n\n1. **For atmosphere:**\n\
  \   - `atmos_axes`: This is an input argument that specifies the array size for\
  \ atmospheric diagnostic fields.\n   - `land_pe`: This is an input argument that\
  \ specifies the land pe number.\n\n2. **For coupling:**\n   - `Time`: This is an\
  \ input argument that represents the current model time.\n   - `Atm`, `Land`, `Ice`,\
  \ `Ocean`: These are derived types that specify boundary data for atmosphere, land,\
  \ ice, and ocean respectively.\n   - `atmos_ice_boundary`, `land_ice_atmos_boundary`,\
  \ `land_ice_boundary`, `ice_ocean_boundary`, `ocean_ice_boundary`: These are input\
  \ arguments that specify the boundaries between different components.\n   - `do_ocean`:\
  \ This is a logical input argument that specifies whether to include ocean in the\
  \ coupling.\n   - `slow_ice_ocean_pelist`: This is an input argument that specifies\
  \ the list of slow ice-ocean processes.\n   - `dt_atmos`, `dt_cpld.time`: These\
  \ are input arguments that specify the time steps for atmosphere and coupling respectively.\n\
  \n3. **For flux calculations:**\n   - The context provides several fields under\
  \ `flux_down_from_atmos` and `flux_up_to_atmos` which are used as inputs to calculate\
  \ fluxes between different components (atmosphere, land, ice, ocean)."
Are the grids for atm and land different?: Yes, the grids for atm (Atm%grid) and land
  are different as indicated by `X1_GRID_LAND = 3` which sets up separate grids for
  ATM (`XMAP_SFC(1)`), ICE (`XMAP_SFC(2)`), and LAND (`XMAP_SFC(3)`).
Why are there so many data_override calls?: 'The numerous `data_override` calls in
  these subroutines serve two primary purposes:


  1. **Data Configuration**: They allow for runtime configuration of fields to be
  overridden based on a data_table. This means users can specify which fields should
  be faked or replaced with constant values or file data without changing the model''s
  source code.


  2. **Model Coupling**: In a coupled climate model like FMSCoupler, different components
  (atmosphere, ocean, land, ice) exchange data at their boundaries. `data_override`
  helps manage these exchanges by ensuring that only specified fields are overridden,
  preventing unwanted modifications to other fields.


  The frequent use of `data_override` is due to the complex and dynamic nature of
  coupled climate models, where flexibility in managing data exchanges between components
  is crucial for model development, testing, and validation.'
Why are there so many checksum subroutines?: 'The numerous checksum subroutines in
  FMSCoupler serve different purposes to ensure data integrity and consistency across
  coupled components (atmosphere, ocean, land, ice) during model simulations. Here''s
  why they exist:


  1. **coupler_chksum_type**: This type contains all checksum-related operations for
  the coupler. It holds a pointer to `coupler_components_type` to simplify argument
  lists when calling checksum subroutines.


  2. **coupler_components_type**: This type is a convenient object that holds pointers
  to all model component derived types and inter-component boundary derived types,
  reducing the length of argument lists in checksum subroutines.


  3. **get_ocean_chksums**: This subroutine prints out checksums for ocean and ice-ocean
  boundary fields. It''s specific to the ocean component and helps monitor data consistency
  within that component.


  4. **coupler_end**, **coupler_summarize_timestep**: These subroutines compute checksums
  when certain flags (`do_chksum` or `do_endpoint_chksum`) are true, helping detect
  any inconsistencies or errors during finalization (in `coupler_end`) and each timestep
  (in `coupler_summarize_timestep`).


  The multiple checksum subroutines allow for:

  - Component-specific checksum verification (`get_ocean_chksums`)

  - Timestep-by-timestep consistency checks (`coupler_summarize_timestep`)

  - Final data integrity verification before model shutdown (`coupler_end`)


  In summary, the various checksum subroutines help maintain data consistency and
  integrity across components and timesteps in the coupled climate model.'
What's different between get_atmos_ice_land_ocean_chksums and get_coupler_chksums?: 'The
  main difference between `get_atmos_ice_land_ocean_chksums` and `get_coupler_chksums`
  lies in the scope of fields they compute checksums for:


  - `get_atmos_ice_land_ocean_chksums`: This subroutine computes checksums specifically
  for atmosphere, fast-ice, and land fields. It does not include ocean fields.


  - `get_coupler_chksums`: This subroutine computes checksums for a broader range
  of fields including Atmosphere (Atm), Land, Ocean, and Ice-Ocean boundary fields
  as listed in its description.'
How do I turn off checksum computations?: To turn off checksum computations, set the
  namelist flag `do_chksum` to `.FALSE.`
How many different pelists are there?: 'There are four different pelists:


  1. `ATMPELIST`: Pelist for atmosphere.

  2. `OCEANPELIST`: Pelist for ocean.

  3. `LANDPELIST`: Pelist for land, which is a subset of `ATMPELIST`.

  4. `ICEFAST_PELIST`: Pelist for fast sea ice, which is also a subset of `ATMPELIST`.


  Additionally, there are two combined pelists:


  1. `Ice%pelist`: Combined list of all ice PEs (union of fast and slow pelists).

  2. `slow_ice_ocean_pelist`: Combined MPI pelist of the slow-ice and ocean PEs.


  So, in total, there are six different pelists.'
What does coupler_set_ice_surface_fields do?: '`coupler_set_ice_surface_fields` prepares
  the ice surface state for atmosphere fast physics and pre-calculates ice radiative
  properties by calling `set_ice_surface_fields` from `ice_model_mod`. It uses the
  Ice component and coupler_clocks as inputs/outputs.'
What does coupler_unpack_ocean_ice_boundary do?: coupler_unpack_ocean_ice_boundary
  unpacks ocean-ice boundary data into the ice model state after coupler_flux_ocean_to_ice.
  It first overrides data if the field exists in data_table, then calls unpack_ocean_ice_boundary
  to unpack the data. If do_chksum is true, it computes slow_ice_chksums.
What does coupler_unpack_land_ice_boundary do?: coupler_unpack_land_ice_boundary prepares
  the fast-ice model to receive new land discharge fields by calling `ice_model_fast_cleanup`
  and `unpack_land_ice_boundary`. It then copies these fields into the ice model's
  internal state.
Where are all the diagnostic fields registered?: "All diagnostic fields are registered\
  \ in `diag_field_init` subroutine, which calls `fms_diag_register_diag_field` for\
  \ each field. The fields registered include:\n\n- Static fields:\n  - `land_mask`\n\
  \  - `height2m`\n  - `height10m`\n  - `sftlf`\n\n- Atmosphere tracer fields (per-tracer):\n\
  \  - `*_tot_con_atm`, `*_tot_con_ref`, `*_atm`, `*_surf`, `*_flux`, `*_ref`, `*_mol_flux`,\
  \ `*_atm_dvmr`, `*_surf_dvmr`, `*_mol_flux_atm0`\n\n- CMIP fields:\n  - `tas`, `uas`,\
  \ `vas`, `sfcWind`, `huss`, `hurs`, `rhs`, `ts`, `psl`, `tauu`, `tauv`, `hfss`,\
  \ `hfls`, `evspsbl`, `tslsi`, `tos`, `sic`"
How do I add a variable to write out to a file?: "To add a variable to write out to\
  \ a file in FMSCoupler, you would typically follow these steps:\n\n1. **Define the\
  \ variable**: First, ensure that the variable is defined and has the appropriate\
  \ type (e.g., real, integer, character) and dimensions.\n\n2. **Open the output\
  \ file**: You need to open the file where you want to write the data. In FMSCoupler,\
  \ this could be done using the `fms2_io` module. For example:\n\n   ```fortran\n\
  \   type(fms2_io), intent(inout) :: io_unit\n   logical, intent(in) :: divert_stocks_report\n\
  \n   if (divert_stocks_report) then\n       call fms2_open_file(io_unit, 'stocks.out',\
  \ action='write')\n   else\n       call fms2_open_file(io_unit, stdout, action='write')\n\
  \   end if\n   ```\n\n3. **Write the variable to the file**: Once the file is open\
  \ and the variable is defined, you can write the data using appropriate I/O statements.\
  \ For example:\n\n   ```fortran\n   real, intent(in) :: my_variable\n\n   call fms2_write(io_unit,\
  \ my_variable)\n   ```\n\n4. **Close the output file**: After writing the data,\
  \ don't forget to close the file.\n\n   ```fortran\n   call fms2_close_file(io_unit)\n\
  \   ```"
What is written out in the restarts?: "In the `coupler_intermediate_restart` subroutine,\
  \ the following are written out to mid-run restart files:\n\n1. **Component model\
  \ restarts**:\n   - Atmosphere, land, and ice restarts on PEs where Atmpe is true.\n\
  \   - Ocean restart on PEs where Oceanis_ocean_pe is true.\n\n2. **Coupler-specific\
  \ boundary-condition restart data**:\n   - Ocn_bc_restart\n   - Ice_bc_restart\n\
  \nAfter writing these files, `Time_restart` is advanced by `restart_interval` to\
  \ set the next scheduled intermediate restart time. If the optional `time_stamp`\
  \ argument is present, it prefixes all file names to allow multiple intermediate\
  \ restart sets to coexist in the RESTART/ directory.\n\nIn the `coupler_restart`\
  \ subroutine, all coupler-owned restart files are written. These include:\n\n1.\
  \ Coupler-specific boundary-condition restart data:\n   - Ocn_bc_restart\n   - Ice_bc_restart\n\
  \n2. Component model restarts (if provided as arguments):\n   - Atmosphere (`atm`)\n\
  \   - Ice (`ice`)\n   - Ocean (`ocean`)\n   - Ocean state (`ocean_state`)"
How do I add a variable to write out to a restart file?: "To add a variable to be\
  \ written out to a restart file in FMSCoupler, you would need to:\n\n1. Define the\
  \ variable and its attributes (e.g., name, type, description) in the appropriate\
  \ module or namelist.\n2. Include the variable in the list of variables that are\
  \ written to the restart file(s). This could involve modifying the `coupler_restart`\
  \ subroutine to add the new variable to the list of ocean fields restarts (`Oceanfields_restart`),\
  \ ice-ocean fluxes restarts (`Iceocean_fluxes_restart`), or other relevant lists.\n\
  3. Ensure that the variable is initialized and updated correctly throughout the\
  \ model run, so that its current state can be accurately saved to the restart file.\n\
  \nHere's an example of how you might add a new real, scalar variable named `new_variable`\
  \ with a description \"A new variable for restart\":\n\n```fortran\n! Add this to\
  \ the appropriate module or namelist\nreal :: new_variable  ! New variable to be\
  \ written to restart files\nlogical :: do_write_new_variable = .false.  ! Namelist\
  \ flag to control writing of new_variable\n\n! Modify coupler_restart subroutine\
  \ to include new_variable in Oceanfields_restart list\nif (do_write_new_variable)\
  \ then\n    Oceanfields_restart%var_list = [Oceanfields_restart%var_list, 'new_variable']\n\
  end if\n\n! Initialize and update new_variable as needed throughout the model run\n\
  \n! Write new_variable to restart file when coupler_restart is called\nif (do_write_new_variable)\
  \ then\n    write(Oceanfields_restart%unit, *) Oceanfields_restart%time, new_variable\n\
  end if\n```"
Is the exchange grid generated only once?: No, the exchange grid is not generated
  only once. It's regenerated based on a clock (`regenclock`) for profiling purposes.
What does coupler_atmos_tracer_driver_gather_data do?: coupler_atmos_tracer_driver_gather_data
  gathers CO2, NH3, and tagged/isotopic NH3 at the bottom atmospheric layer in preparation
  for flux exchange with other components (ocean, land, ice). It calls atmos_tracer_driver_gather_data
  to perform this task.
What does coupler_update_atmos_model_state do?: coupler_update_atmos_model_state updates
  atmospheric state and diagnostic fields in Atm at the end of the atmospheric timestep,
  calling update_atmos_model_state from atmos_model. It measures runtime using coupler_clocksupdate_atmos_model_state.
  If do_chksum is true, it computes checksums; if do_debug is true, it prints memory
  usage.
What is the difference between atmos_ocean_fluxes_calc and atmos_ocean_dep_fluxes_calc?: 'The
  difference between `atmos_ocean_fluxes_calc` and `atmos_ocean_dep_fluxes_calc` lies
  in the type of fluxes they compute:


  - `atmos_ocean_fluxes_calc`: Computes non-deposition gas fluxes between atmosphere
  and ocean. These are generic fluxes that can be calculated using different implementations
  like OCMIP2, DUCE, or Johnson''s method.


  - `atmos_ocean_dep_fluxes_calc`: Specifically calculates deposition gas fluxes between
  atmosphere and ocean. Deposition fluxes refer to the transfer of gases from the
  atmosphere to the ocean surface due to wet and dry deposition processes.'
are ocean and ice on the same grid?: Yes, Ice and Ocean must share the same physical
  grid.
Which is most commonly used?  REGRID, REDIST, or DIRECT?: DIRECT
How do I turn off land_flux_to_ice?: To turn off the transfer of freshwater discharge
  from the land model to the ice/ocean grid, you need to set `do_runoff` namelist
  flag to `.FALSE.`. This will disable the interpolation of land runoff onto the ice
  grid via the subroutine `flux_land_to_ice()`.
